<% content_for :title, "Agents" %>

<%# Avoid N+1 slug lookups in results table %>
<% prefetch_canonical_slugs(entity_type: "agent", entity_ids: @agents.map { |a| a["agent_id"] }) %>
<% prefetch_canonical_slugs(entity_type: "agency", entity_ids: @agents.map { |a| a["agency_id"] }) %>

<main class="max-w-5xl mx-auto px-4 py-6 space-y-6">
  <%= render partial: "entities/shared/entity_header", locals: {
    title: "Agents",
    subtitle: "Agent directory (from pcms.agents).",
    breadcrumbs: [["Agents", nil]],
    search_scope: "agents",
    search_query: @query,
    search_placeholder: "Search agents (name or agency)…"
  } %>

  <% if @query.blank? %>
    <p class="text-sm text-muted-foreground">
      Showing a small sample. Search to narrow.
    </p>
  <% end %>

  <section class="space-y-3">
    <h2 class="text-xs font-medium uppercase tracking-wide text-muted-foreground">Results</h2>

    <% if @agents.empty? %>
      <div class="p-4 rounded-lg bg-muted/30 border border-border/50 text-sm text-muted-foreground italic">No agents found.</div>
    <% else %>
      <div class="overflow-x-auto rounded-lg border border-border">
        <table class="min-w-full text-sm">
          <thead class="bg-muted/50 text-xs text-muted-foreground">
            <tr>
              <th class="text-left px-3 py-2 font-medium">Agent</th>
              <th class="text-left px-3 py-2 font-medium">Agency</th>
              <th class="text-right px-3 py-2 font-medium">agent_id</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-border">
            <% @agents.each do |a| %>
              <tr class="hover:bg-muted/30">
                <td class="px-3 py-2">
                  <a href="<%= agent_href(a['agent_id']) %>" class="font-medium hover:underline"><%= a["full_name"] %></a>
                  <% if a["is_active"] == false %>
                    <span class="ml-2 text-xs px-1.5 py-0.5 rounded bg-muted text-muted-foreground">inactive</span>
                  <% end %>
                </td>
                <td class="px-3 py-2">
                  <% if a["agency_id"].present? && a["agency_name"].present? %>
                    <a href="<%= agency_href(a['agency_id']) %>" class="hover:underline"><%= a["agency_name"] %></a>
                  <% elsif a["agency_name"].present? %>
                    <span><%= a["agency_name"] %></span>
                  <% else %>
                    <span class="text-muted-foreground">—</span>
                  <% end %>
                </td>
                <td class="px-3 py-2 text-right font-mono tabular-nums text-muted-foreground"><%= a["agent_id"] %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      <p class="text-xs text-muted-foreground">
        Links prefer canonical slugs when known; otherwise they use the numeric fallback (<code>/agents/:id</code>) which creates/promotes a canonical slug on demand.
      </p>
    <% end %>
  </section>
</main>
